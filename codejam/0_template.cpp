#include <bits/stdc++.h>

using namespace std;



void solve(int c)
{
    int ans = -1;
    int len =0;
    for(int i=0;i<n;i++)
    {
        cout<<A[i]<<"\n";
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t,n,k;
    cin >> t;
    cin >> n;
    int A[n];
    for(int i=0; i<n;i++)
        cin>> A[i];
    cin >>k;
    
    for (int i = 0; i < t; ++i)
    {
        solve(t);
    }

    return 0;
}
