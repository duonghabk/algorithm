#include <bits/stdc++.h>

using namespace std;

int t,n,k;

void solve(int *A)
{
    
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    cin >> t;
    cin >> n;
    int A[n];
    for(int i=0; i<n;i++)
        cin>> A[i];
    cin >>k;
    
    for (int i = 0; i < t; ++i)
    {
        solve(A);
    }

    return 0;
}
